 for (int i = 0; i < (*nFutP); ++i) {

        if (strcmp((*futPlantillas)[i].idPlantilla, (*userPlantillas)[input].idPlantilla) == 0) {
            strcpy((*futPlantillas)[i].idPlantilla, "999");
        }
    }


    menuParticipante("01", "Jesus", &arrayConfig, &arrayFutbolista, &arrayEquipo, &arrayPlantilla, &arrayFutP,
                        &nPlantilla, &nFutbolista, &nEquipo, &nFutP);

                        loginres(&nEquipo, &nFutbolista, &nUsuario, &nPlantilla, &nFutP, nConfig, &arrayEquipo, &arrayFutbolista,
                            &arrayUsuario, &arrayPlantilla, &arrayFutP, &arrayConfig);